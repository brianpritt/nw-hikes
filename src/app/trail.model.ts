export class Trail {
  constructor (public name: string, public elevation_gain: string, public region: string, public lat: number, public long: number, public intro: string, public description: string, public length: string, public difficulty: string, public picture: string) {}
}
